<?xml version="1.0" encoding="utf-8" ?>

<toolkit:Popup
    x:Class="WorkoutDiary.Views.PickerPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit">

    <ScrollView>
        <Frame BorderColor="Black" >


            


    <VerticalStackLayout Padding="20" Spacing="10"  Background="White" >
                <SearchBar x:Name="Search" CancelButtonColor="#8cca68" Placeholder="Szukaj"
               TextChanged="OnSearch"/>

                <CollectionView x:Name="ListView" 
                    SelectionMode="Single"
                    SelectionChanged="OnSelected">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" 
                               ItemSpacing="8"/>
            </CollectionView.ItemsLayout>


                    <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Border Padding="12" 
                        Margin="2"
                        Stroke="#8cca68"
                        StrokeShape="RoundRectangle 10">

                        <!--<Label Text="{Binding}" FontSize="14"/>-->
                                <Grid ColumnDefinitions="auto,*"
              ColumnSpacing="10">

                                    <!-- 🔹 tekst -->
                            <Label Grid.Column="0" TextColor="black"
                   Text="{Binding}"
                   VerticalOptions="Center"
                   FontSize="14"/>

                        </Grid>
                    </Border>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

        </VerticalStackLayout>

    </Frame>
    </ScrollView>
</toolkit:Popup>